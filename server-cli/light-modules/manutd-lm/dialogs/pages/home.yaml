label: Home Page
form:
  layout:
    $type: tabbedLayout
    tabs:
      - name: tabMain
        fields:
          - name: title
          - name: footer
      - name: tabMeta
        fields:
          - name: meta
  properties:
    title:
      label: Title
      $type: textField
    meta:
      label: Code
      $type: codeField
      language: html
    footer: # This one is for the reference a footer data, not a component, so there is no mgnl:template
      $type: linkField
      label: Footer
      datasource:
        $type: jcrDatasource
        workspace: footers
      chooser:
        workbenchChooser:
          appName: footers-app
      textInputAllowed: false
      converterClass: info.magnolia.ui.editor.converter.JcrNodeToIdentifierConverter
      #converterClass: info.magnolia.ui.editor.converter.JcrNodeToPathConverter
